services:
  - type: web
    name: bee-scenario-ai
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: DEEPSEEK_API_KEY
        sync: false
